export enum ROUTES_ENUM {
  HOME = "/",
  ACTIVITY = "/activity",
  CONTACTS = "/contacts",
  DEMINING = "/demining",
  ADMIN = "/admin/",
  AUTH = "auth",
  RESTORE = "restore",
  NOT_FOUND = "/*",
}
