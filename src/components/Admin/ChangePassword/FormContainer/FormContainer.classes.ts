import { SxProps } from "@mui/material";

const BoxWrapperStyle: SxProps = {
  display: "flex",
  alignItems: "start",
  justifyContent: "center",
  minHeight: "580px",
};

const BoxContainerStyle: SxProps = {
  display: "flex",
  maxWidth: "390px",
};

const PaperStyle: SxProps = {
  padding: "50px",
};

export { BoxContainerStyle, BoxWrapperStyle, PaperStyle };
