import { SxProps } from "@mui/material";

const BoxStyles: SxProps = {
  marginBlock: "20px",
  display: "flex",
  justifyContent: "center",
};

const LinkStyles: SxProps = {
  textDecoration: "none",
  color: "inherit",
};

export { BoxStyles, LinkStyles };
